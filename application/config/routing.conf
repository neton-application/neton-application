# 路由组配置（前缀对齐 docs/rbac-spec.md §10）
debug = false

[[groups]]
group = "admin"
mount = "/admin-api"
requireAuth = true

[[groups]]
group = "app"
mount = "/app-api"
requireAuth = true

[[groups]]
group = "open"
mount = "/platform-api"
